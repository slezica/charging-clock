<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">


    <uses-permission android:name="android.permission.WRITE_SETTINGS"
                     tools:ignore="ProtectedPermissions"/>

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Package visibility (API 30+): without these, PackageManager can't see
         Samsung's AOD package or resolve the lock screen settings intent, and
         the AOD-style Fix action would silently never appear. -->
    <queries>
        <package android:name="com.samsung.android.app.aodservice" />
        <intent>
            <action android:name="android.settings.LOCK_SCREEN_SETTINGS" />
        </intent>
    </queries>

    <application
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <service
                android:name=".services.AmbientTileService"
                android:icon="@drawable/ic_phonelink_setup_black_24dp"
                android:label="Charging Clock"
                android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
                android:exported="true">

            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE"/>
            </intent-filter>
        </service>

        <service
            android:name=".services.AmbientControlService"
            android:exported="true"
            android:stopWithTask="false"
            android:foregroundServiceType="specialUse">

            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Toggles Always On Display while the device is charging"/>
        </service>

    </application>
</manifest>